import { actionTypes } from './action'

const initialState = {
  creator: {},
}

export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
